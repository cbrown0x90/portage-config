--- a/Makefile.kmk	2020-02-18 18:10:41.000000000 +0100
+++ b/Makefile.kmk	2020-02-27 20:40:38.492800826 +0100
@@ -191,8 +191,8 @@
  if1of ($(KBUILD_TARGET), linux solaris)
   ifndef VBOX_ONLY_BUILD
    ifneq ($(VBOX_GCC_VERSION_CXX),)
-    ifeq ($(int-ge $(VBOX_GCC_VERSION_CXX),40400),)
-     $(error gcc >= 4.4 required when compiling against Qt5!)
+    ifeq ($(int-ge $(VBOX_GCC_VERSION_CXX),10000),)
+     $(error gcc >= 1.0 required when compiling against Qt5!)
     endif
    endif
   endif
